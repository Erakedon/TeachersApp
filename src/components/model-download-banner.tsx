/**
 * Model-download progress bar — stub for Stage 8.
 *
 * In Stage 8, useLLM will be wired inside the day-plan generation screen
 * rather than the root layout, and download progress will be shown there.
 * This component is kept as a placeholder so that existing imports continue
 * to compile cleanly.
 */

import React from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-function
export function ModelDownloadBanner() {
  return null;
}
